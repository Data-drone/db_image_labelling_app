# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# dist/ is intentionally tracked so Databricks Apps can deploy directly from Git
# (package.json is nested under frontend/ so Databricks won't auto-run npm build).
# Rebuild before committing when frontend source changes: `npm run build`


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
